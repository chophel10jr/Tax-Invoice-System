<%= render 'layouts/header' %>

<div class="h-auto flex justify-center">
  <div class="w-[700px] px-[32px] py-[16px]">
    <div class="rounded-[22px] bg-[#F6F6F6]">
      <div class="p-[32px] overflow-y-auto">
        <div class="text-center mb-4 pb-[16px] border-b border-[rgba(0,0,0,0.1)]">
          <h1 class="text-2xl font-bold text-bnb_blue leading-tight">
            Login
          </h1>
        </div>

        <div>
          <%= form_with(url: login_path, method: :post) do |form| %>
            <p class="mb-[8px]">Email</p>
            <div class="form-group mb-[24px]">
              <%= form.email_field :email, class: "form-input", placeholder: ' ' %>
              <%= form.label :email, "Email", class: 'form-label' %>
            </div>

            <p class="mb-[8px]">Password</p>
            <div class="form-group bg-[24px]">
              <%= form.password_field :password, class: 'form-input', placeholder: ' ' %>
              <%= form.label :password, "Password", class: 'form-label' %>
            </div>

            <div>
              <%= form.submit "Login", class: 'btn-blue-bnb w-full mt-[24px]' %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
